# site-noise
To run this project locally, you will need to set a JWT Key using: dotnet user-secrets set "Jwt:Key" "your_key_here"
Then run backend using "dotnet run" in the backend folder. To run the frontend use "npm start" in the frontend folder.

This project uses C# .NET for the backend and React for the frontend.
